import { Accordion, AccordionDetails, AccordionSummary, Box, styled } from "@mui/material";

export const AccordionBudgetStyle = styled(Accordion)`
    width: 100%;
    max-width: 1400px;
    display: flex;
    flex-direction: row;
    
`

export const AccordionBudgetSummary = styled(AccordionSummary)`
        display: flex;
        flex-direction: row;
        gap: 20px;
        width: 100%;
        max-width: 900px;
        padding: 0;
`
export const AccordionBox = styled(Box)`
        display: flex;
        flex-direction: row;
        gap: 10px;

`
