import { createTheme } from '@mui/material';

// Create a custom theme with the desired purple color
export const theme = createTheme({
  palette: {
    primary: {
      main: '#9c27b0', // Your desired purple color
    },
  },
});